name: Update Cache

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 13 * * *" # CST 21:00 - full (pdb + bgpq4)
    - cron: "0 1 * * *" # CST 9:00 - pdb only
  workflow_dispatch:
    inputs:
      mode:
        description: "Cache mode"
        required: true
        default: "full"
        type: choice
        options:
          - full
          - pdb-only
          - bgpq4-only

permissions:
  contents: write

jobs:
  update-cache:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout config repo
        uses: actions/checkout@v4

      - name: Checkout scripts repo
        uses: actions/checkout@v4
        with:
          repository: kawaiinetworks/as27523-vyos-scripts
          path: scripts

      # bgpq4 is a system tool (not on conda-forge); Python deps come from pixi.
      - name: Install bgpq4
        run: |
          sudo apt-get update
          sudo apt-get install -y bgpq4

      - name: Setup pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          manifest-path: scripts/pixi.toml
          cache: true

      - name: Determine mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "flag=${{ github.event.inputs.mode == 'pdb-only' && '--pdb-only' || github.event.inputs.mode == 'bgpq4-only' && '--bgpq4-only' || '' }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event.schedule }}" = "0 1 * * *" ]; then
            # CST 9:00 = pdb-only
            echo "flag=--pdb-only" >> "$GITHUB_OUTPUT"
          else
            # CST 21:00 or push = full
            echo "flag=" >> "$GITHUB_OUTPUT"
          fi

      - name: Update cache
        working-directory: scripts
        env:
          ARIN_API_KEY: ${{ secrets.ARIN_API_KEY }}
        run: pixi run python configure/save-cache.py "$GITHUB_WORKSPACE" ${{ steps.mode.outputs.flag }}

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cache/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update cache [automated]"
            git push
          fi
